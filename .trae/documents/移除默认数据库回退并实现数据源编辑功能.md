I will implement the following changes to address your requests:

### 1. Enforce Datasource Management (Remove Default Fallback)
*   **Refactor `src/utils/db.py`**:
    *   Modify `QueryDatabase.__init__` to remove the fallback logic that reads from `QUERY_DB_*` environment variables.
    *   If no `datasource` object is provided, it will raise an error instead of defaulting to a hardcoded database.
    *   This ensures all queries must go through a configured `DataSource` from the management system.

### 2. Add "bird测试" Data Source
*   **Action**: Once the backend supports the update API and the fallback is removed, I will use the `create_datasource` API to add the PostgreSQL source ("bird测试") with the provided credentials (`159.75.148.55:5432`, `queryuser`, `123456`, `postgres`).

### 3. Implement "Edit Data Source" Functionality
*   **Backend (`src/server.py`)**:
    *   Add a new `PUT /api/datasources/{id}` endpoint to support updating existing data sources.
*   **Frontend (`src/pages/DataSourcePage.tsx`)**:
    *   Add an "Edit" button to each row in the Data Source table.
    *   Update the Modal logic to handle both "Create" and "Edit" modes.
    *   When editing, pre-fill the form with the selected data source's details.
    *   Ensure "Test Connection" works for the edited values before saving.

### Verification
*   Verify `QueryDatabase` fails if no datasource is provided.
*   Verify the new "bird测试" datasource appears in the list.
*   Verify that editing a datasource (e.g., changing the port or password) works and persists.
