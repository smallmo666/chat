I will implement the "Test Connection" feature for data sources.

### 1. Backend Implementation (`src/server.py`)
*   **Define Model**: Create a `DataSourceTest` Pydantic model with fields: `type`, `host`, `port`, `user`, `password`, `dbname`.
*   **Add Endpoint**: Implement `POST /api/datasources/test` that:
    *   Accepts connection parameters.
    *   Attempts to create a database connection using `sqlalchemy`.
    *   Returns success or failure message.

### 2. Frontend Implementation (`src/pages/DataSourcePage.tsx`)
*   **Update UI**: Add a "Test Connection" button to the "Add Data Source" modal form.
*   **Add Logic**: Implement `handleTestConnection` function to:
    *   Validate form fields (excluding 'name').
    *   Call the test endpoint.
    *   Show success/error toast notifications.

### 3. Verification
*   Restart backend server.
*   Open frontend "Add Data Source" modal.
*   Fill in valid/invalid credentials and click "Test Connection" to verify behavior.
